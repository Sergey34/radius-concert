spring:
  application:
    geocode:
      file:
        path: event/src/main/resources/geocodes.xml
    events:
      dataset:
        refresh: '0 0 0 * * ?'

  mongodb:
    embedded:
      storage:
        database-dir: ./.db

  profiles:
    active: load_geocode_config
logging:
  level:
    org.springframework: info
